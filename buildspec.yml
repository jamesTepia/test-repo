version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      
  build:
    commands:
      - echo "Building the project..."
      
  post_build:
    commands:
      - echo "Running tests..."
